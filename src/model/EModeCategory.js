class EModeCategory {

    constructor(id, name, liquidationThreshold) {
        this.id = id;
        this.name = name;
        this.liquidationThreshold = liquidationThreshold;
    }

}

module.exports = EModeCategory;